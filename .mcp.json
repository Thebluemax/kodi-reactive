{
  "mcpServers": {
    "memory": {
      "type": "stdio",
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "-v",
        "claude-memory:/app/dist",
        "mcp/memory"
      ],
      "env": {}
    }
  }
}
